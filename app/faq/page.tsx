import type { Metadata } from "next";
import InfoPage from "@/components/InfoPage";
export const metadata: Metadata = { title: "YouTube Downloader FAQ", description: "Answers about supported YouTube URLs, privacy, formats, permissions, and ClipFetch downloads.", alternates: { canonical: "https://clipfetch.in/faq" } };
const faqs = [["Is ClipFetch free?","The frontend is free to use. Any limits or costs depend on the connected service."],["Which links are supported?","Only public YouTube watch, Shorts, embed, and youtu.be video URLs, including standard www and m variants. Other sites and unsupported YouTube URL shapes are rejected."],["Do you store my links?","The frontend does not store link history. The configured backend receives a YouTube URL to analyze or download it; review its deployment policy."],["Can I download anything?","Only content you have the right or permission to save. Respect YouTube’s terms and copyright."],["Why can’t I analyze a link?","Configure NEXT_PUBLIC_API_URL with the ClipFetch API and check that yt-dlp is installed on the API host."]];
export default function Page() { const jsonLd = { "@context": "https://schema.org", "@type": "FAQPage", mainEntity: faqs.map(([name, text]) => ({ "@type": "Question", name, acceptedAnswer: { "@type": "Answer", text } })) }; return <><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /><InfoPage title="Frequently asked questions" intro="Straight answers about ClipFetch’s YouTube-only workflow."><div className="divide-y divide-ink/10 rounded-2xl border border-ink/10 bg-white px-6">{faqs.map(([q,a]) => <details className="py-5" key={q}><summary className="cursor-pointer font-bold">{q}</summary><p className="mt-3 leading-7 text-ink/60">{a}</p></details>)}</div></InfoPage></>; }
